unit uEnumeradores;

interface

type
  tMetodos = (tmGET, tmPOST, tmPUT, tmDELETE);

  tTiposContentType = (tctApplicationJSON, tctAplicationXML, tctAplicationImage);

  tAccept = (taUTF8);

  tIncDec = (tidInc, tidDec);

implementation

end.

